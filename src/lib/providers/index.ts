/**
 * Provider de IA ativo.
 * Para trocar de provider, altere o import abaixo.
 * Ex: import { openaiProvider as activeProvider } from './openai';
 */
export { groqProvider as activeProvider } from './groq';
